export class AuthService {
  checkAuth(): Promise<boolean> {
    //TODO: build
    return Promise.resolve(false);
  }

  getUser(): Promise<any> {
    //TODO: build
    return Promise.resolve({});
  }
}
